je veux que :

- l'api qui va passer le audio par slide soit separer donc ne va pas être ici : interface Slide {
                                                                                    id: number;
                                                                                    title: string;
                                                                                    script: string;
                                                                                    audioData?: string; // Base64 encoded audio dat

- l'api va passer l'audio en binaire

- n'integrer pas tts dans cette interface

- selement tu recoit l'audio correspondant à la slide et tu le lit avec la slide quand il est affiché